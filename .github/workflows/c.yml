name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Install CMake
      run: apt-get install cmake
    - uses: actions/checkout@v2
    - name: cmake
      run: cmake .
    - name: make test
      run: make test
